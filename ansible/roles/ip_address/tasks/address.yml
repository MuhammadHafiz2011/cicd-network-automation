---

# ===========================
# IP ADDRESS CONFIGURATION
# ===========================

# -------------------------------
# GET CURRENT IP CONFIGURATION
# -------------------------------

- name: Get current IP configuration
  community.routeros.command:
    commands:
      - /ip address export terse
# Menyimpan konfigurasi IP Address statis yang sedang aktif pada MikroTik.
  register: current_ip
  changed_when: false

# ---------------------------
# ADD OR UPDATE IP ADDRESS
# ---------------------------
# Menambahkan IP baru atau memperbarui interface apabila konfigurasi berbeda.

- name: Add or Update IP address
  community.routeros.command:
    commands:
      - >
        :local ipAddr "{{ item.ip_address }}";
        :local iface "{{ item.interface }}";

        :local existing [/ip address find where address=$ipAddr];

        :if ([:len $existing] = 0) do={

          /ip address add \
          address=$ipAddr \
          interface=$iface;

        } else={

          :if (
            [/ip address get $existing interface] != $iface
          ) do={

            /ip address set \
            $existing \
            interface=$iface;

          }

        }

  loop: >-
    {{
      mikrotik_interfaces
      | selectattr('ip_address', 'defined')
      | list
    }}

  loop_control:
    label: "{{ item.interface }} -> {{ item.ip_address }}"

# -----------------------------------
# REFRESH CURRENT IP CONFIGURATION
# -----------------------------------
# Memperbarui state IP Address setelah proses Add/Update agar proses Build Existing menggunakan konfigurasi terbaru.

- name: Refresh current IP configuration
  community.routeros.command:
    commands:
      - /ip address export terse
  register: current_ip
  changed_when: false

# -------------------------
# BUILD EXISTING IP LIST
# -------------------------
# Menyusun daftar IP Address yang saat ini terdapat pada MikroTik.

- name: Build existing IP list
  set_fact:
    existing_ips: >-
      {{
        current_ip.stdout | join('\n')
        | regex_findall('address=([0-9./]+)')
      }}

# -----------------------------------------------------
# BUILD DESIRED IP LIST
# -----------------------------------------------------
# Menyusun daftar IP Address berdasarkan konfigurasi pada inventory.

- name: Build desired IP list
  set_fact:
    desired_ips: >-
      {{
        mikrotik_interfaces
        | selectattr('ip_address', 'defined')
        | map(attribute='ip_address')
        | list
      }}

# ---------------------------
# REMOVE UNUSED IP ADDRESS
# ---------------------------
# Menghapus IP Address yang tidak lagi terdapat pada inventory.

- name: Remove unused IP address
  community.routeros.command:
    commands:
      - /ip address remove [find address="{{ item }}"]

  loop: "{{ existing_ips | difference(desired_ips) }}"

  when:
    - (existing_ips | difference(desired_ips)) | length > 0

  loop_control:
    label: "Removing {{ item }}"
