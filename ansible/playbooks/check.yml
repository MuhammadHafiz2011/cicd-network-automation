- name: CI Check - Validate MikroTik Configuration
  hosts: mikrotik
  gather_facts: no
  connection: network_cli

  tasks:
    - name: Check connectivity to MikroTik
      community.routeros.command:
        commands:
          - /system identity print
      check_mode: yes
