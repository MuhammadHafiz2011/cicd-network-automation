---
# ===================================
# Validate MikroTik Configuration
# ===================================
# Playbook untuk menjalankan proses validasi konfigurasi
# sebelum deployment dilakukan.

- name: Validate MikroTik Configuration

  hosts: mikrotik

  gather_facts: no

  roles:

    - validate
